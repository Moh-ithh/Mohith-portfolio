# Three.js & WebGL (`three`)

This directory houses WebGL orchestrations, shader scripts, custom geometry generators, and scene management for three-dimensional interactions.

## Guidelines
- Centralize setup for Canvas systems, custom render loops, shaders (e.g. GLSL files), and interactive particles.
